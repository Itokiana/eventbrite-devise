<nav class="navbar navbar-expand-lg navbar-dark shadow" style="background-color: #373634">
  <a class="navbar-brand" href="<%= root_path%>">Eventbrite Like⛵😇 </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link" href="<%= root_path %>">Accueil</a>
      </li>
      <%if user_signed_in? %>
      <li class="nav-item">
        <a class="nav-link" href="<%= new_event_path %>">Créer un évènement</a>
      </li>
      <%end%>
    </ul>
    <form class="form-inline">
      <%if user_signed_in? %>
      <%= link_to "Mon profile", user_show_path(current_user.id), class: 'btn btn-outline-warning' %>&nbsp;
      <%= link_to "Déconnexion", destroy_user_session_path, method: :delete, class: 'btn btn-outline-danger my-2 my-sm-0' %>
      <%else%>
      <a href="<%= new_user_session_path %>" class="btn btn-outline-success my-2 my-sm-0">Se connecter ou s'inscrire?</a>
      <%end%>
    </form>
  </div>
</nav>